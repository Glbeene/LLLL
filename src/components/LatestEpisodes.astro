---
const episodes = [
  {
    number: 1,
    title: "Garrett & Jackie",
    description: "Welcome to the very first episode of the Limitless Livin' Marriage Podcast! In this episode, we introduce ourselves, share our story, and open up about why we're starting this podcast.",
    href: "/episodes"
  }
];
---
<section class="py-20 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-display font-bold text-center mb-12">Latest Episodes</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {episodes.map(episode => (
        <a 
          href={episode.href}
          class="bg-gray-50 rounded-lg p-6 hover:bg-gray-100 transition duration-200"
        >
          <div class="space-y-3">
            <div class="text-sm font-semibold text-gray-500">Episode {episode.number}</div>
            <h3 class="text-xl font-display font-bold">{episode.title}</h3>
            <p class="text-gray-600">{episode.description}</p>
          </div>
        </a>
      ))}
      <div class="bg-gray-50 rounded-lg p-6 flex items-center justify-center">
        <p class="text-gray-500 text-center">More episodes coming soon!</p>
      </div>
    </div>
  </div>
</section>